<Window x:Class="ClientAPI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:ClientAPI.ViewModels"
        xmlns:local="clr-namespace:ClientAPI"
        mc:Ignorable="d"
        Title="API Client .NET 8" Height="600" Width="900"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        WindowState="Maximized"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0" 
                      ItemsSource="{Binding HttpMethods}" 
                      SelectedItem="{Binding SelectedMethod}"
                      materialDesign:HintAssist.Hint="Método"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,0,8,0"/>

            <TextBox Grid.Column="1" 
                     Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="URL da Requisição"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,8,0"/>

            <Button Grid.Column="2" 
                    Command="{Binding ExecuteRequestCommand}"
                    Content="{materialDesign:PackIcon Kind=Send}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"/> 
            </Grid>

        <GroupBox Grid.Row="1" Header="Request Body (JSON)" 
                  FontWeight="Bold"
                  Style="{StaticResource MaterialDesignCardGroupBox}">
            <TextBox Text="{Binding RequestBody}"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     VerticalContentAlignment="Top"
                     Height="Auto" MinHeight="100"/>
        </GroupBox>

        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    Margin="0,10">
            <TextBlock Text="Status: " FontWeight="Bold"/>
            <TextBlock Text="{Binding StatusCode}" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            <ProgressBar 
                 Style="{StaticResource MaterialDesignCircularProgressBar}"
                 Value="0" IsIndeterminate="True"
                 Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                 Margin="5,0,0,0" Height="20" Width="20"/>
        </StackPanel>

        <GroupBox Grid.Row="3" Header="Response" 
                  FontWeight="Bold"
                  Style="{StaticResource MaterialDesignCardGroupBox}">
            <TextBox Text="{Binding ResponseBody}"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     VerticalContentAlignment="Top"/>
        </GroupBox>
    </Grid>
    
</Window>